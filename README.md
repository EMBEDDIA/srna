# SRNA - Semantics-aware recurrent neural nets

This paper contains the code to reproduce the paper's results.

Run benchmarks as:

```
python3 evaluate.py --hybridRNN True --core_dataset reuters
```

for the example reuters data set.